const apis =  {
  GetEmployeePermission2: {
    action: 'GetEmployeePermission',
    service: 'rbac'
  },
};
// Object.keys(apis).forEach(name => {
  // apis[name].permission = 'APermissionApis';
// });
export default apis;